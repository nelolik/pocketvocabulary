<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextOriginWord" parent="AppTheme">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextTranslationWord" parent="AppTheme">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="TextOriginYandexResponse" parent="TextOriginWord" >
        <item name="android:textSize">24sp</item>
        <item name="android:layout_marginStart">@dimen/text_margin</item>
        <item name="android:layout_marginLeft">@dimen/text_margin</item>
        <item name="android:layout_marginTop">@dimen/text_margin</item>
        <item name="android:layout_marginBottom">@dimen/text_margin_top_bottom</item>
    </style>

    <style name="TextTranslationYandexResponse" parent="AppTheme">
        <item name="android:textSize">24sp</item>
        <item name="android:layout_marginStart">@dimen/text_margin</item>
        <item name="android:layout_marginLeft">@dimen/text_margin</item>
        <item name="android:layout_marginTop">@dimen/text_margin_top_bottom</item>
        <item name="android:layout_marginBottom">@dimen/text_margin_top_bottom</item>
    </style>

</resources>
